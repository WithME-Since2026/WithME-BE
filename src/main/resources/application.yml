spring:
  application:
    name: WithME
  # 프로필을 지정하지 않고 실행하면 로컬 개발용(dev) 설정을 사용한다.
  profiles:
    default: dev
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
